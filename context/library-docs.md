# Library Docs

## Supabase Auth
- Used for Next.js authentication via `@supabase/auth-helpers-nextjs` or `@supabase/ssr`.

## shadcn/ui
- Primary component library. Do not build from scratch what shadcn provides.

## Redis
- Used as pub-sub between Rust nodes and Next.js / widget connections.
